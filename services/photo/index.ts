/**
 * Photo Upload Services Entry Point
 * Handles photo capture, compression, and upload functionality
 */

export { default as PhotoUploadService } from './photoUploadService';
export type { PhotoMetadata, PhotoUploadResult, CompressionOptions } from './photoUploadService';
